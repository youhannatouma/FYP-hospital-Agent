# Preferred single connection string (takes precedence when set)
DATABASE_URL=

# Fallback DB_* settings when DATABASE_URL is not set.
# Canonical runtime path: Docker network (backend -> db on 5432)
# If you run backend directly on host instead, use DB_HOST=localhost and DB_PORT=5433.
DB_HOST=db
DB_PORT=5432
DB_NAME=FYP
DB_USER=postgres
DB_PASSWORD=

SECRET_KEY=
ALGORITHM=HS256
POSTGRES_PASSWORD=
CLERK_SECRET_KEY=
CORS_ORIGINS=
